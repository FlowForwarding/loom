{application, simple_ne,
 [
  {description, ""},
  {vsn, "1"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib,
                  lager,
                  folsom,
                  of_driver,
                  ofs_handler
                 ]},
  {mod, { simple_ne_app, []}},
  {env, [
    {stats, [flow, table, aggregate, port, queue, group, meter]},
    {stats_interval, 10} % 'disable' to disable
  ]}
 ]}.
